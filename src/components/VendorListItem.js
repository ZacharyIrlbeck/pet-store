// add the number of listed pets to their cards

function VendorListItem({ vendor }){
    return(
        <div>
            { vendor.displayname }
        </div>
    )
}

export default VendorListItem 